第一期：客户和服务器进行交互，分别实现几个命令，客户端发命令，服务器端接收命令，进行处理，然后将结果返回给客户端。
第二期：在第一期的基础上进行完善，实现所述说的功能
第三期：实现数据库操作，进行注册和登录，也就是在发命令前执行这个卡住的操作，通过这个操作才能进行命令操作。
       然后就是虚拟目录的设计，每个用户有一个文件夹，这个要拼接到路径上。然后就是上传文件的时候，要用md5码去查询数据库，是否
       存在数据库中，如果不存在，就上传文件，并添加进数据库上，重点是找到precode值，当是/目录的时候就是0，其他的时候就是上一级
       的code值，所以这点判断好基本就实现了问题。妙传是存在的时候，只用进行硬链接然后进行存入数据库即可
数据库设计：表一：userid（主键） username  salt
           表二：code  （主键） precode name type md5 user abspath
 
